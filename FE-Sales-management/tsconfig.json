{
  "compilerOptions": {
    "baseUrl": ".", // Set base URL to the root of the project
    "paths": {
      "@app/*": ["src/*"] // Map `@app` to `src`
    }
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
