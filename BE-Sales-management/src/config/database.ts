import { ConnectionOptions } from "mysql2";

const dbConnfig: ConnectionOptions = {
  host: "localhost",
  user: "root",
  password: "2001",
  database: "sales_management",
};

export default dbConnfig;
